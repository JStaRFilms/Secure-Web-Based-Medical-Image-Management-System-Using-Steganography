generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model EncryptionEvent {
  id        String   @id @default(cuid())
  userId    String?
  operation String
  details   String?
  timestamp DateTime @default(now())
}
